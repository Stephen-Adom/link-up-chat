import TableRecentCalls from "../components/Calls/TableRecentCalls";

const RecentCalls = () => {
  return (
    <section id="Recent-Calls">
      <TableRecentCalls />
    </section>
  );
};

export default RecentCalls;
