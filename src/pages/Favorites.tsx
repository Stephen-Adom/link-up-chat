import FavoriteContactTable from "../components/Favorites/FavoriteContactTable";

const Favorites = () => {
  return (
    <section id="favorites-contact">
      <FavoriteContactTable />
    </section>
  );
};

export default Favorites;
